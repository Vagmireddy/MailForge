export async function notifyRateLimit(user:any,sender:string){if(!user.slackAccessToken)return;await fetch('https://slack.com/api/chat.postMessage',{method:'POST',headers:{Authorization:`Bearer ${user.slackAccessToken}`,'Content-Type':'application/json'},body:JSON.stringify({channel:user.slackTeamId,text:`Hourly email limit reached for ${sender}. Remaining emails moved to the next hour.`})});}
